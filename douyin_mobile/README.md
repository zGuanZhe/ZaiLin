# Stickman vs Physics Mobile

这是当前主项目的移动端复制版，不再使用旧移动端独立实现。

- `game.js`：从主项目当前版本复制而来。
- `index.html` / `styles.css`：主项目入口基础上增加触控按钮层。
- `mobile-controls.js`：把按钮触摸转换成原游戏使用的键盘事件。

直接打开本目录的 `index.html` 即可运行。
